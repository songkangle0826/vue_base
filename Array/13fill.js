/*
* fill() 方法用于将一个固定值替换数组的元素。
* 语法:
*   objArray.fill(value,start,end);
* 参数:
*   value:  要填充的内容
*   start: 要填充开始的地方
*   end: 填充结束的地方(不包括此位置)
* 返回值:
*   返回填充后的数组
* 是否改变原数组
*   改变原数组
* */

let arr = [1,2,43,4,5,6];
// console.log(arr.fill('10'),arr)

// console.log(arr.fill(10,3))

// console.log(arr.fill(10,3,5))
